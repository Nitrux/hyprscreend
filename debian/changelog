hyprscreend (0.0.5)  nitrux; urgency=medium

  * The external daemon now adds any connected monitor that is not the internal panel, instead of only handling names from the hardcoded external allowlist.

 -- Uri Herrera <uri_herrera@nxos.org>  Sat, 04 Jul 2026 21:44:00 -0500

hyprscreend (0.0.4)  nitrux; urgency=medium

  * Wait for the internal monitor to appear before applying the initial
    refresh mode in hyprscreend, so first-login battery sessions do not miss
    the low-refresh configuration.
  * Wait briefly for external monitor mode data before configuring a newly
    detected output in hyprextscreend, so first-login races do not skip the
    initial external display setup.

 -- Uri Herrera <uri_herrera@nxos.org>  Wed, 24 Jun 2026 00:00:00 -0500

hyprscreend (0.0.3)  nitrux; urgency=medium

  * Preserve the user's existing monitor position in hyprextscreend instead of
    always rewriting it as "auto", which clobbered manually set positions on
    every login.
  * Relax the up-to-date check so a monitor block with any non-empty position
    (not just "auto") is considered already configured and is not rewritten.

 -- Uri Herrera <uri_herrera@nxos.org>  Sat, 23 May 2026 12:00:00 -0500

hyprscreend (0.0.2)  nitrux; urgency=medium

  * Fix external monitor matching in hyprextscreend to use exact output names
    instead of regex substring matching.
  * Skip Crystal Dock restart in greetd/greeter sessions to avoid launching it
    in the login Hyprland instance.

 -- Uri Herrera <uri_herrera@nxos.org>  Mon, 18 May 2026 00:10:00 -0500

hyprscreend (0.0.1)  nitrux; urgency=medium

  * Create package.

 -- Uri Herrera <uri_herrera@nxos.org>  Sun, 17 May 2026 22:32:00 -0500
